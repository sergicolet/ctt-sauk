{
    "nodes": [
        {
            "parameters": {
                "jsCode": "/* Algoritmo Optimizado O(n días) */\nfunction calcBusinessHours(startDate, endDate) {\n    const start = new Date(startDate);\n    const end = new Date(endDate);\n    let totalHours = 0;\n    let currentDay = new Date(start);\n    currentDay.setHours(0, 0, 0, 0);\n    \n    while (currentDay <= end) {\n        const dayOfWeek = currentDay.getDay();\n        if (dayOfWeek !== 0 && dayOfWeek !== 6) { // L-V\n            const dayStart = Math.max(currentDay.getTime(), start.getTime());\n            const dayEnd = Math.min(new Date(currentDay).setHours(23, 59, 59, 999), end.getTime());\n            totalHours += (dayEnd - dayStart) / (1000 * 60 * 60);\n        }\n        currentDay.setDate(currentDay.getDate() + 1);\n    }\n    return Math.floor(totalHours);\n}\n\nreturn {\n  ...$json,\n  hours_in_status: calcBusinessHours($json.shipping_status_datetime, new Date().toISOString())\n};"
            },
            "id": "calc-horas-opt",
            "name": "Calc_Horas_O_n_days",
            "type": "n8n-nodes-base.code",
            "typeVersion": 2,
            "position": [
                400,
                200
            ]
        },
        {
            "parameters": {
                "rules": {
                    "values": [
                        {
                            "conditions": {
                                "conditions": [
                                    {
                                        "leftValue": "={{ $json.shipping_status_code }},{{ $json.hours_in_status }}",
                                        "operator": {
                                            "type": "string",
                                            "operation": "regex"
                                        },
                                        "rightValue": "^(0700|0900|1000|1100|1200|3900),(2[4-9]|[3-9]\\d|\\d{3,})$"
                                    }
                                ]
                            },
                            "renameOutput": true,
                            "outputKey": "Transito_24h_Laborables"
                        },
                        {
                            "conditions": {
                                "conditions": [
                                    {
                                        "leftValue": "={{ $json.shipping_status_code }},{{ $json.hours_in_status }}",
                                        "operator": {
                                            "type": "string",
                                            "operation": "regex"
                                        },
                                        "rightValue": "^(1700|1800|38\\d{2}|3901),(2[4-9]|[3-9]\\d|\\d{3,})$"
                                    }
                                ]
                            },
                            "renameOutput": true,
                            "outputKey": "Aduanas_24h_Laborables"
                        }
                    ]
                }
            },
            "id": "switch-estados-opt",
            "name": "Switch_Estados_SLA_Strict",
            "type": "n8n-nodes-base.switch",
            "typeVersion": 3,
            "position": [
                650,
                200
            ]
        }
    ],
    "connections": {
        "Calc_Horas_O_n_days": {
            "main": [
                [
                    {
                        "node": "Switch_Estados_SLA_Strict",
                        "type": "main",
                        "index": 0
                    }
                ]
            ]
        }
    }
}